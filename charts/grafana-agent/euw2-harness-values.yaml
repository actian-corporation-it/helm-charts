global:
  externalLabels:
    cloudProvider: "aws"
    cluster: "<+pipeline.stages.avalanchedataplaneekscluster.spec.execution.steps.apply_euw2.output.eks_main_cluster_name>"
  environment: "<+pipeline.stages.euw2_grafana_agent.spec.infrastructure.environmentRef>"
  region: <+<+pipeline.stages.euw2_grafana_agent.identifier>.substring(0,1)>